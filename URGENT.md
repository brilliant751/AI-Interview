# URGENT 待办（Agent5 维护）

- 最后更新：2026-05-04
- 依据评审：简历管理与面试回放最终评审（Agent5）
- 维护规则：仅保留“未完成”且短周期必须推进的事项；已落地项在下次 review 时删除。

## P0（阻断放行）

无。

## P1（高优先）

### 1. `POST /api/v1/resumes` 契约与实现状态码不一致
- 当前状态：未完成
- 现状证据：
  - OpenAPI 定义 `POST /api/v1/resumes` 返回 `201`（`openapi/openapi.yaml`）
  - 实际接口返回 `200`（Agent4 回归与缺陷记录）
- 完成标准：
  - 统一实现与契约（推荐实现改为 `201`），并同步 OpenAPI 与 Postman
  - 回归测试断言恢复为单一状态码，不再兼容 `200/201`

### 2. 覆盖率门禁缺少量化证据，无法验证 80% 目标
- 当前状态：未完成
- 现状证据：
  - 当前测试命令未输出覆盖率百分比（Agent4 报告已明确）
  - 无法对照 AGENTS.md 的覆盖率门槛（>=80%）
- 完成标准：
  - 后端与前端测试命令接入覆盖率统计并在 CI 输出
  - 报告中提供各层覆盖率数值并与 80% 目标对比

### 3. 后端运行环境与项目声明版本仍不一致（Python 3.9 vs `>=3.11`）
- 当前状态：未完成
- 现状证据：
  - `backend/pyproject.toml` 声明 `requires-python = ">=3.11"`
  - 当前本地 `.venv` 仍为 Python 3.9（测试虽可执行但存在兼容性隐患）
- 完成标准：
  - 开发与 CI 统一至 Python 3.11+
  - 在 3.11+ 环境下完成全量回归并沉淀环境基线文档

## P2（中优先）

### 4. 本地 provider 客户端受系统代理影响
- 当前状态：未完成
- 现状证据：
  - provider/下载链路默认 httpx 客户端可能继承代理环境
  - 在代理场景可能引发本地 provider 初始化或回归不稳定
- 完成标准：
  - 明确本地 provider 是否继承代理；若不需要则默认禁用代理继承
  - 代理环境下补一条稳定性回归用例
