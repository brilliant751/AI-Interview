# URGENT 待办（Agent5 维护）

- 最后更新：2026-05-10
- 更新依据：当前分支已完成 `rebase main`，并执行最小验证（后端单测、前端单测、前端构建、Playwright E2E）全部通过；以下仅保留仍未完成 urgent 事项
- 维护规则：仅保留“当前未完成”且短周期必须推进的事项。

## P0（阻断放行）

无。

## P1（高优先）

### 1. `POST /api/v1/resumes` 契约与实现状态码不一致
- 当前状态：未完成
- 现状证据：
  - OpenAPI 定义 `POST /api/v1/resumes` 返回 `201`
  - 线上实现仍存在返回 `200` 的路径（历史回归已记录）
- 完成标准：
  - 统一实现与契约（推荐统一为 `201`）
  - 同步 OpenAPI 与 Postman
  - 回归测试断言收敛为单一状态码

### 2. 覆盖率门禁缺少量化证据，无法验证 80% 目标
- 当前状态：未完成
- 现状证据：
  - 当前 CI/本地常用测试命令不输出覆盖率统计
  - 无法对照 AGENTS.md 的覆盖率目标（>=80%）
- 完成标准：
  - 后端与前端测试链路接入覆盖率统计
  - 在 CI 报告中输出覆盖率百分比并与 80% 目标对照

### 3. 运行环境与项目声明版本不一致（Python 3.9 vs `>=3.11`）
- 当前状态：未完成
- 现状证据：
  - `backend/pyproject.toml` 声明 `requires-python = ">=3.11"`
  - 当前开发环境仍存在 Python 3.9 路径
- 完成标准：
  - 开发与 CI 统一到 Python 3.11+
  - 在 3.11+ 环境完成全量回归并沉淀环境基线

## P2（中优先）

### 1. 本地 provider 客户端是否继承系统代理仍缺少明确策略
- 当前状态：未完成
- 现状证据：
  - 本地 provider/下载链路在代理环境下可能继承代理设置，稳定性不确定
- 完成标准：
  - 明确是否继承代理（需要则文档化，不需要则默认禁用）
  - 在代理环境补一条可复现的稳定性回归

### 2. `followup` 模式当前为占位语义，仍需产品化收敛
- 当前状态：未完成
- 现状证据：
  - 接口已对外暴露 `mode="followup"`
  - 当前实现返回 `followup_placeholder`，未形成真实追问逻辑
- 完成标准：
  - 明确短期策略：要么返回未支持（4xx），要么补齐可用追问链路
  - 与前端能力说明保持一致，避免契约歧义
